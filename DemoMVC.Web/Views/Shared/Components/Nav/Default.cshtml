@using DemoMVC.Domain
@model IEnumerable<Category>

@Html.ActionLink("Home", "GetProducts", "Demo"
                       , new { page = 1}
                       , new { @class = "btn btn-block" + 
                       (ViewBag.CurrentCategory == null ? " btn-primary": "")
                })

@foreach (var category in @Model)
{
    string strCategory = Enum.GetName<Category>(category) ?? "";
    @Html.RouteLink(strCategory.ToString(), new {
        controller = "Demo",
        action = "GetProducts",
        category = category,
        page = 1
    }, new { @class = "btn btn-block m-1" + 
             (category == ViewBag.CurrentCategory? " btn-primary" : "")
           })
}
